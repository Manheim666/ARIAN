# ── Core scientific stack ──────────────────────────────────────────────
numpy>=1.26,<2.2
pandas>=2.2
polars>=1.5                  # faster joins over the 5 M+ row fire tables
pyarrow>=17                  # parquet + geoparquet backend
duckdb>=1.1                  # ad-hoc analytical SQL over parquet partitions

# ── Geospatial ─────────────────────────────────────────────────────────
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
rasterio>=1.3
rioxarray>=0.17
xarray>=2024.7
zarr>=2.18                   # chunked cubes for grid × time × var
fiona>=1.10
pyogrio>=0.10                # geopandas vector IO engine (much faster)
contextily>=1.6              # basemaps for notebooks only
h3>=4.1                      # hex indexing for partitioning

# ── OSM / KMZ / archive handling ───────────────────────────────────────
pyrosm>=0.6.2                # OSM PBF parser (uses libosmium under the hood)
fastkml>=1.0

# ── Open-Meteo + HTTP ──────────────────────────────────────────────────
httpx>=0.27
tenacity>=9.0                # retry decorators
aiofiles>=24.1
openmeteo-requests>=1.3      # official client w/ flatbuffer support
requests-cache>=1.2

# ── Orchestration & config ─────────────────────────────────────────────
prefect>=3.0
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0

# ── ML (later phases, declared now so venv is frozen once) ─────────────
scikit-learn>=1.5
xgboost>=2.1
lightgbm>=4.5
optuna>=4.0
mlflow>=2.17

# ── Dev ───────────────────────────────────────────────────────────────
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.6
